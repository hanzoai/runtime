name: runtime volume list
synopsis: List all volumes
usage: runtime volume list [flags]
options:
  - name: format
    shorthand: f
    usage: Output format. Must be one of (yaml, json)
inherited_options:
  - name: help
    default_value: 'false'
    usage: help for runtime
see_also:
  - runtime volume - Manage Runtime volumes
