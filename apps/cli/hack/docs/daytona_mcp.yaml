name: runtime mcp
synopsis: Manage Runtime MCP Server
description: Commands for managing Runtime MCP Server
inherited_options:
  - name: help
    default_value: 'false'
    usage: help for runtime
see_also:
  - runtime - Runtime CLI
  - runtime mcp config - Outputs JSON configuration for Runtime MCP Server
  - 'runtime mcp init - Initialize Runtime MCP Server with an agent (currently supported: claude, windsurf, cursor)'
  - runtime mcp start - Start Runtime MCP Server
