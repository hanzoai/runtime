name: runtime mcp init
synopsis: |
  Initialize Runtime MCP Server with an agent (currently supported: claude, windsurf, cursor)
usage: runtime mcp init [AGENT_NAME] [flags]
inherited_options:
  - name: help
    default_value: 'false'
    usage: help for runtime
see_also:
  - runtime mcp - Manage Runtime MCP Server
